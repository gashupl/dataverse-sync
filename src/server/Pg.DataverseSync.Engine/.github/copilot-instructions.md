# Copilot Instructions

## Project Guidelines
- Always use {} braces for conditional expressions in this repository/folder.
- When implementing logging use PascalCase for named placeholders.
- New classes (both private and public ones) should be always in the separate files. Always use one class = one file approach. 